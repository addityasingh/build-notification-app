import React from 'react'
import { NotifListView } from 'routes/NotifList/components/NotifListView'
import { render } from 'enzyme'

describe('(View) NotifList', () => {
  let _component

  // beforeEach(() => {
  //   _component = render(<NotifListView />)
  // })

  // it('Renders a list of notification messages', () => {
  //   const welcome = _component.find('div')
  //   const welcomeImg = _component.find('img')
  //   expect(welcome).to.exist
  //   expect(welcomeImg).to.exist
  // })
})
